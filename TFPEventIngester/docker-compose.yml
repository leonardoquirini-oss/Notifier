version: '3.8'

services:
  tfpeventingester:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tfp-event-ingester
    hostname: tfp-event-ingester
    ports:
      - "8095:8080"   # Web Console    
    networks:
      - berlink-network
    volumes:
      - /opt/berlink/tfp-event-ingester/data/application.yml:/app/application.yml
      
    restart: unless-stopped

networks:
  berlink-network:
    external: true
    name: berlink-dev_berlink-network
