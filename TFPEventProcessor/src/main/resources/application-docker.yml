# Docker Environment Overrides
# Activated with: SPRING_PROFILES_ACTIVE=docker

spring:
  artemis:
    broker-url: tcp://${ARTEMIS_HOST:artemis}:${ARTEMIS_PORT:61616}

  datasource:
    url: jdbc:postgresql://${DB_HOST:172.28.234.122}:${DB_PORT:5432}/${DB_NAME:berlinkdb}

logging:
  level:
    root: INFO
    com.containermgmt.tfpeventprocessor: INFO
    org.javalite.activejdbc: WARN
